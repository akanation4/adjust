import { useEffect } from "react";

export default function RoomPage() {

    useEffect(() =>  {
        if (didL)
        const pass = window.location.state.pass;

        )

    return (
        
        );
}