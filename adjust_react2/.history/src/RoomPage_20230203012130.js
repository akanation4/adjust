import { useEffect } from "react";

export default function RoomPage() {

    useEffect(() =>  {
        const pass = window.location.href.split("/").pop();
        )

    return (
        
        );
}